using UnityEngine;

namespace OkapiKit
{
    public class CombatTextPivot : OkapiElement
    {
        public override string GetRawDescription(string ident, GameObject refObject)
        {
            return "Defines where the combat text spawns when triggered.";
        }
    }
}
